<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FileExplorer.UI.UserControls.CommandedInfoBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileExplorer.UI.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:FileExplorer.UI.Behaviors"
    mc:Ignorable="d">

    <InfoBar Margin="0 0 4 4"
             VerticalAlignment="Bottom" HorizontalAlignment="Right"
             IsOpen="{x:Bind ViewModel.IsOpen, Mode=OneWay}" 
             Severity="{x:Bind ViewModel.Severity, Mode=OneWay}"
             Title="{x:Bind ViewModel.Title, Mode=OneWay}"
             Message="{x:Bind ViewModel.Message, Mode=OneWay}" />
</UserControl>
