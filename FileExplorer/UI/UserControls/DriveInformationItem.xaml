<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FileExplorer.UI.UserControls.DriveInformationItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileExplorer.UI.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="300"> 
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2.5*" />
        </Grid.ColumnDefinitions>

        <Image Source="{x:Bind Drive.Thumbnail.Source, Mode=OneWay}" Height="40" />

        <Grid Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="1.4*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="{x:Bind Drive.FriendlyName, Mode=OneWay}" />

            <ProgressBar Grid.Row="1" Value="{x:Bind Drive.DriveSpace.AvailablePercentage, Mode=OneWay}" Height="20" />

            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock Text="{x:Bind Drive.DriveSpace.FreeSpace, Mode=OneWay}" />
                <TextBlock Text="free out of" Margin="5 0" />
                <TextBlock Text="{x:Bind Drive.DriveSpace.TotalSpace, Mode=OneWay}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
